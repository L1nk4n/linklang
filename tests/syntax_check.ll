Function myFunc {
    let x = 5;
}

Class MyClass {

}

Enum MyEnum {

}

Struct MyStruct {

}

For (i8 i = 0; i < x.length; i++) {
    let y = i;
}

While (x > y) {
    let y = i;
}

Print(x);
Print(5);
Print(x, y, 10);
Print(x.length);
PrintLn(x);
PrintLn(5);
PrintLn(x, y, 10);
PrintLn(x.length);
Return a;
Exit;
TypeOf(x);
TypeOf(5);
TypeOf(x, y, 10);
SizeOf(x);
SizeOf(5);
SizeOf(x, y, 10);
WriteFile(path, "string");
ReadFile(path);
AppendFile(path, "string");
Error("test");
Warn("test");
ReadLine();
Input();
Open(path, "mode");
Close(handle);
